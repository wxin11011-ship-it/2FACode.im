---
import { Icon } from 'astro-icon/components';
import Logo from '~/components/Logo.astro';
import ToggleTheme from '~/components/common/ToggleTheme.astro';
import Button from '~/components/ui/Button.astro';

import { getHomePermalink } from '~/utils/permalinks';
import { getAsset } from '~/utils/permalinks';
import type { CallToAction } from '~/types';

interface Link {
  text?: string;
  href?: string;
  ariaLabel?: string;
  icon?: string;
}

interface MenuLink extends Link {
  links?: Array<MenuLink>;
}

export interface Props {
  id?: string;
  links?: Array<MenuLink>;
  actions?: Array<CallToAction>;
  isSticky?: boolean;
  isDark?: boolean;
  isFullWidth?: boolean;
  showToggleTheme?: boolean;
  showRssFeed?: boolean;
  showFeedback?: boolean;
  githubUrl?: string;
  position?: string;
}

const {
  id = 'header',
  links = [],
  actions = [],
  isSticky = false,
  isDark = false,
  isFullWidth = false,
  showToggleTheme = false,
  showRssFeed = false,
  showFeedback = true,
  githubUrl = '',
  position = 'left',
} = Astro.props;

const hasNavLinks = links.some((link) => Boolean(link.text));
const iconButtonClass =
  'inline-flex h-10 w-10 items-center justify-center rounded-lg text-muted dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 focus:outline-none focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700';
---

<header
  class:list={[
    { sticky: isSticky, relative: !isSticky, dark: isDark },
    'top-0 z-40 flex-none mx-auto w-full border-b border-gray-50/0 transition-[opacity] ease-in-out',
  ]}
  {...isSticky ? { 'data-aw-sticky-header': true } : {}}
  {...id ? { id } : {}}
>
  <div class="absolute inset-0"></div>
  <div
    class:list={[
      'relative text-default py-2 px-4 sm:px-6 mx-auto w-full flex items-center justify-between gap-3',
      {
        'max-w-5xl': !isFullWidth,
      },
    ]}
  >
    <div class="flex items-center min-w-0">
      <a class="flex items-center" href={getHomePermalink()}>
        <Logo />
      </a>
    </div>

    {
      hasNavLinks ? (
        <nav
          id="site-navigation"
          class="hidden items-center text-default lg:flex"
          aria-label="Main navigation"
        >
          <ul class="flex flex-row items-center text-[0.9375rem] tracking-[0.01rem] font-medium">
            {links.map(({ text, href }) =>
              text && href ? (
                <li>
                  <a
                    class="hover:text-link dark:hover:text-white px-3 py-2 flex items-center whitespace-nowrap"
                    href={href}
                  >
                    {text}
                  </a>
                </li>
              ) : null
            )}
          </ul>
        </nav>
      ) : null
    }

    <div
      class:list={[
        'flex items-center gap-0.5 sm:gap-1 shrink-0',
        { 'ml-auto': position === 'left' || !hasNavLinks },
      ]}
      data-header-actions
    >
      {
        githubUrl ? (
          <a
            class={iconButtonClass}
            href={githubUrl}
            target="_blank"
            rel="noopener noreferrer"
            aria-label="View open-source code on GitHub"
            title="Open source on GitHub"
          >
            <Icon name="tabler:brand-github" class="w-5 h-5" />
          </a>
        ) : null
      }

      {
        showFeedback ? (
          <button
            type="button"
            data-feedback-trigger
            class={iconButtonClass}
            aria-label="Send feedback"
            title="Send feedback"
          >
            <Icon name="tabler:message-circle" class="w-5 h-5" />
          </button>
        ) : null
      }

      {showToggleTheme && <ToggleTheme iconClass="w-5 h-5" class={iconButtonClass} />}

      {
        showRssFeed ? (
          <a class={iconButtonClass} aria-label="RSS Feed" href={getAsset('/rss.xml')}>
            <Icon name="tabler:rss" class="w-5 h-5" />
          </a>
        ) : null
      }

      {
        actions?.length ? (
          <span class="ml-1 hidden sm:inline-flex">
            {actions.map((btnProps) => (
              <Button {...btnProps} class="ml-1 py-2 px-4 font-semibold shadow-none text-sm w-auto" />
            ))}
          </span>
        ) : null
      }
    </div>
  </div>
</header>
